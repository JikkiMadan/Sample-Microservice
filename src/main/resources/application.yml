server:
  port: 8080
spring:
  application:
    name: sample-microservice-server
eureka:
  client:
    register-with-eureka: true
    fetch-registry: false 
    service-url:
      defaultZone: http://localhost:8761/eureka
